var eucidean_norm = function(v) {
    var sum_squares = 0.0;
    for (var i = 0; i < v.length; i += 1)
        sum_squares += v[i] * v[i];
    return Math.sqrt(retval);
}

if (right < left) {
    tmp = right;
    right = left;
    left = tmp;
}

